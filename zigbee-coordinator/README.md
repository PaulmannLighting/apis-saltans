# zigbee-coordinator

This library aims to provide a simple API for controlling Zigbee devices.

It is built on top of the [zigbee-hw](../zigbee-hw) library, which provides a hardware abstraction layer for Zigbee
hardware.

The coordinator layer provides a high-level API for Zigbee devices, including support for device discovery, binding, and
reporting.