# TODOs

- [ ] Incorporate message backchannel into `NcpDriver` trait, so that drivers can send messages back to the coordinator.
- [x] Implement `NcpDriver` trait as an external API for drivers to implement, and for the coordinator to use.