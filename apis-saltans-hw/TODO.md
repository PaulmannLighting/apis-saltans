# TODOs

- [ ] Implement start/stop API to allow restarting the hardware during program's runtime.
- [x] Add a method to `Driver` to allow spawning the actor and returning an `Ncp` proxy object.
- [x] Implement `Driver` as the external command API for hardware backends.
