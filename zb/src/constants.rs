//! Common constants.

/// Amount of deci-seconds per millisecond.
pub const DECI_SECONDS_PER_MILLISECOND: u64 = 100;

/// Maximum size of the bytes array.
pub const U8_CAPACITY: usize = (u8::MAX - 1) as usize;
