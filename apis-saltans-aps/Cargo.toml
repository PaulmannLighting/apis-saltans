[package]
name = "apis-saltans-aps"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "2"
le-stream = { version = "10", features = ["derive"] }
log = "0.4"
num-derive = "0.4"
num-traits = "0.2"
apis-saltans-core = { path = "../apis-saltans-core" }

[lints]
workspace = true
